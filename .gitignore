# macOS / Windows
.DS_Store
Thumbs.db

# Node / dependency caches
node_modules/
*.log
npm-debug.log*

# Local env files
.env
.env.local
.env.*.local

# Netlify build cache (regenerated on every deploy — never commit)
.netlify/

# Netlify zip-deploy temp artifacts (created locally by @netlify/mcp upload)
deploy-*.zip

# Editor / IDE
.vscode/
.idea/
*.swp
*~
